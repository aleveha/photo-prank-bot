{
	"name": "photo-prank-bot",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "NODE_ENV=development bun run src/scripts/concurrently.ts",
		"build": "bun run db:m && next build && bun run startup",
		"start": "next start",
		"format": "biome format --write .",
		"db:g": "bun run --env-file=.env.local drizzle-kit generate --name",
		"db:m": "bun run --bun drizzle-kit migrate",
		"db:ui": "bun run --env-file=.env.local drizzle-kit studio",
		"startup": "bun -e '(await import(\"~/scripts/startup.ts\")).startup();'"
	},
	"dependencies": {
		"@grammyjs/auto-retry": "^2.0.2",
		"@grammyjs/ratelimiter": "^1.2.0",
		"@vercel/speed-insights": "^1.0.12",
		"drizzle-orm": "^0.33.0",
		"envalid": "^8.0.0",
		"grammy": "^1.29.0",
		"next": "14.2.6",
		"postgres": "^3.4.4",
		"react": "^18",
		"react-dom": "^18"
	},
	"devDependencies": {
		"@biomejs/biome": "1.8.3",
		"@types/bun": "^1.1.8",
		"@types/node": "^20",
		"@types/react": "^18",
		"@types/react-dom": "^18",
		"drizzle-kit": "^0.24.2",
		"localtunnel": "^2.0.2",
		"typescript": "^5"
	}
}
